<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlternatorRegulator">

        <activity
            android:name=".ResultsActivity"
            android:exported="false"
            android:label="Results &amp; Curves" />
        <activity
            android:name=".SCTDataActivity"
            android:exported="false"
            android:label="Short Circuit Test (SCT)" />
        <activity
            android:name=".OCTDataActivity"
            android:exported="false"
            android:label="Open Circuit Test (OCT)" />
        <activity
            android:name=".RatedDataActivity"
            android:exported="true"
            android:label="Alternator Regulator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>