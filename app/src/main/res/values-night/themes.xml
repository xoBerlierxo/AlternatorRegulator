<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AlternatorRegulator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/warm_gray_dark</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/warm_accent</item>
        <item name="android:windowBackground">@color/dark_text</item>
        <item name="android:textColorPrimary">@color/warm_white</item>
    </style>
</resources>